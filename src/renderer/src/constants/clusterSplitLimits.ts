/** Minimum width (px) for each pane when two clusters are shown side by side. */
export const CLUSTER_SPLIT_PANEL_MIN_PX = 360

/** Minimum width (px) for each pane in resource tab split within a cluster. */
export const RESOURCE_TAB_SPLIT_PANEL_MIN_PX = 300

/** Minimum width (px) for the resource list when the detail panel is open. */
export const RESOURCE_TABLE_LIST_MIN_PX = 340

/** Minimum width (px) for the resource detail panel beside the list. */
export const RESOURCE_TABLE_DETAIL_MIN_PX = 280
